<div class="follows-wrap">
  <p class="fw text">フォロー list</p>
  <div class="fw-area">
    <div class="list follows">
      <% @users_following.each do |user| %>
        <%= link_to user.nickname, user_path(user), class:"fw-nickname" %>
      <% end %>
    </div>
  </div>
</div>